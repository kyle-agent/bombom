# .env.example — every required environment variable with a PLACEHOLDER value.
# Copy to .env (gitignored) and fill in real values locally. Never commit .env.
#
# Rule: real secrets live only in .env / the deployment environment — never in source.

# [TODO] Add variables as the project takes shape, e.g.:
# DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@localhost:5432/bombom
# ANTHROPIC_API_KEY=your-anthropic-key-here
# PORT=3000
